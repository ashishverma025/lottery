$(document).ready(function(){

	//---------Validate login form------------------
	/*$("#login_form").validate({
		// Specify the validation rules

        rules: {
            email: {
                required: true,
                email: true
            },
            password: "required"    
        },
            
        
        // Specify the validation error messages
        messages: {

             email: {
                required: "<label><font color='red'>*Email Required</font></label>",
                email: "<label><font color='red'>*Enter valid Email</font></label>"
            },
            password: "<label><font color='red'>*Password Required</font></label>"
        }
	});*/
	/*$("#forget_pass").validate({
		// Specify the validation rules

        rules: {
            email: {
                required: true,
                email: true
            }    
        },
  
        // Specify the validation error messages
        messages: {

             email: {
                required: "<label><font color='red'>*Email Is Required</font></label>",
                email: "<label><font color='red'>*Please Enter valid Email</font></label>"
            }
        }
	});*/
//---------Validate set password form------------------
/*$("#password-form").validate({
    rules: {
      user_password: {	
            required: true
           },
      password_confirmation : {
        equalTo:"#new_password",
		required: true
      }
    },
	// Specify the validation error messages
	messages: {
		 user_password: {	
            required: "<label><font color='red'>*Password is Required</font></label>"
           },
		  password_confirmation : {
			equalTo:"<label><font color='red'>*Confirm Password does not matched</font></label>",
			required: "<label><font color='red'>*Confirm Password is Required</font></label>"
		  }
	}
  });*/
});